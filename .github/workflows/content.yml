name : testing Release production workflow
on: push
env:
    GATSBY_CPU_COUNT: 2
    GATSBY_ENV: production
    DIRECTUS_AUTH_TOKEN: ${{ secrets.DIRECTUS_AUTH_TOKEN }}
    GATSBY_ENV_CIO_SITE_ID: ${{ secrets.GATSBY_ENV_CIO_SITE_ID }}
jobs:
    lint:
        timeout-minutes: 10
        runs-on: ubuntu-latest
        environment: production
        
        steps:
            
           # - name: Clone latest tag
           #   uses: actions/checkout@v2.3.1
           #   with:
            #   ref: master
            - run: |
                  git -c 'versionsort.suffix=-' ls-remote --tags --sort=creatordate https://github.com/vijay-deriv/deriv-com
            - name: Set the value
              id: step_one
              run: |
                   echo "$GIT_TAG_NAME"
                   
                   
            - name: Use the value
              id: step_two
              run: |
                    
                  
